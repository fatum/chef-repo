#!/bin/sh
exec 2>&1
cd <%= node.graphiti.base %>
export RACK_ENV=development
exec chpst -u www-data:www-data -l /srv/graphiti/lock -- bundle exec unicorn_rails -p <%= node.graphiti.port %> -E <%= node.chef_environment %> -c /srv/graphiti/config/unicorn.rb
