#!/bin/sh
exec 2>&1
cd <%= node.graphiti.base %>
export RACK_ENV=development
exec chpst -u www-data:www-data -l <%= node.graphiti.base %>/lock -- bundle exec unicorn_rails -p <%= node.graphiti.port %> -E development -c <%= node.graphiti.base %>/config/unicorn.rb
